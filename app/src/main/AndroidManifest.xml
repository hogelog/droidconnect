<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.DroidConnect">

        <!-- Sentry crash reporting. The DSN is injected at build time from
             the SENTRY_DSN environment variable (see app/build.gradle.kts).
             When unset, the placeholder expands to "" and the SDK skips
             initialization, so local builds without Sentry configured work
             unchanged. DSN is a public client-side value per Sentry's
             guidance and is intentionally stored in GitHub Actions vars,
             not secrets. -->
        <meta-data android:name="io.sentry.dsn" android:value="${sentryDsn}" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.TerminalActivity"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="keyboard|keyboardHidden|screenSize|orientation" />

    </application>

</manifest>
