<?xml version="1.0" encoding="utf-8"?>
<!-- Aux button background for the FAB speed dial. Same fills as bg_aux_modifier
     plus a 1dp colorOutlineVariant stroke; adjacent strokes inside the dial
     card act as thin inter-cell borders without needing a divider mechanism
     (GridLayout has no divider attribute). -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_activated="true">
        <shape android:shape="rectangle">
            <solid android:color="?attr/colorPrimary" />
            <stroke android:width="1dp" android:color="?attr/colorOutlineVariant" />
        </shape>
    </item>
    <item>
        <ripple android:color="?attr/colorControlHighlight">
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="?attr/colorSurfaceContainerHigh" />
                    <stroke android:width="1dp" android:color="?attr/colorOutlineVariant" />
                </shape>
            </item>
        </ripple>
    </item>
</selector>
